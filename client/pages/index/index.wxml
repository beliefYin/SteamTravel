<import src="/wxSearch/wxSearch.wxml" />
<!--搜索框-->
<view class="wxSearch-section">
  <view class="wxSearch-pancel">
    <input bindinput="wxSearchInput" bindfocus="wxSerchFocus" value="{{wxSearchData.value}}" bindblur="wxSearchBlur" class="wxSearch-input" placeholder="搜索" />
    <button class="wxSearch-button" bindtap="wxSearchFn" size="mini" plain="true">搜索</button>
  </view>
</view>
<template is="wxSearch" data="{{wxSearchData}}" />

<!--推荐-->
<view class='item' wx:for="{{recommendList}}" bindtap="openScenicSpots" data-url="{{item.url}}">
  <view ><image src="{{item.picUrl}}" class='picture'></image></view>
  <view><text class="cityName">{{item.cityName}}</text></view>
  <view><text class="introduction">{{item.introduction}}</text></view>

</view>